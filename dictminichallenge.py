#A dictionary is a box with labeled item ( key: value)

#create a dictionary
server = {
    "ip" : "192.168.1.1","status" : "online"
}

# #read a value using its label [key]
# print(server["ip"])

server["port"] = 80
server["status"] = "down"